# ===========================================================================
# SehatSaathi-AI — Python Dependencies
# ===========================================================================
# Pin major versions for reproducibility. Update carefully and re-test.
# Run: pip install -r requirements.txt
# ===========================================================================

# ---------------------------------------------------------------------------
# Phase 1: Core backend & frontend
# ---------------------------------------------------------------------------
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# Frontend
streamlit>=1.40.0,<2.0.0

# Utilities
python-dotenv>=1.0.0,<2.0.0
python-multipart>=0.0.12,<1.0.0       # multipart/form-data file uploads

# ---------------------------------------------------------------------------
# Phase 1: Database foundation (prepared, not yet actively used)
# ---------------------------------------------------------------------------
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.14.0,<2.0.0

# ---------------------------------------------------------------------------
# Phase 1: Testing
# ---------------------------------------------------------------------------
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
httpx>=0.28.0,<1.0.0                  # async test client for FastAPI

# ---------------------------------------------------------------------------
# Phase 2: Document processing  ✅ Active
# ---------------------------------------------------------------------------
PyMuPDF>=1.24.0,<2.0.0               # primary PDF text extraction
pdfplumber>=0.11.0,<1.0.0            # fallback PDF extraction (tables)
Pillow>=11.0.0,<12.0.0               # image I/O + test fixtures
opencv-python-headless>=4.10.0,<5.0.0  # image preprocessing (no GUI)
numpy>=1.26.0,<3.0.0                 # array operations for OCR pipeline

# PaddleOCR — install separately due to size & platform-specific wheels:
#   pip install paddlepaddle paddleocr
#
# macOS (Apple Silicon):   pip install paddlepaddle paddleocr
# Linux CPU:               pip install paddlepaddle paddleocr
# Linux GPU (CUDA 11.7):   pip install paddlepaddle-gpu==2.6.1 paddleocr

# ---------------------------------------------------------------------------
# Phase 3: Medical NLP & NER  ✅ Active
# ---------------------------------------------------------------------------
rapidfuzz>=3.9.0,<4.0.0              # fuzzy test-name matching
pandas>=2.2.0,<3.0.0                 # tabular data manipulation (lab tables)

# spaCy — run: python -m spacy download en_core_web_sm
spacy>=3.7.0,<4.0.0

# SciSpaCy (optional — improves biomedical NER accuracy):
#   pip install scispacy
#   pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_md-0.5.4.tar.gz

# ---------------------------------------------------------------------------
# Phase 4: Medical Analysis Engine  ✅ Active
# ---------------------------------------------------------------------------
pyyaml>=6.0.0,<7.0.0                 # load reference range YAML configs

# ---------------------------------------------------------------------------
# Phase 5: Gemini AI layer  ✅ Active
# ---------------------------------------------------------------------------
google-genai>=1.0.0,<2.0.0            # Official Google GenAI SDK (Gemini 2.5 Flash)

# ---------------------------------------------------------------------------
# Phase 6: RAG pipeline  ✅ Active
# ---------------------------------------------------------------------------
faiss-cpu>=1.9.0,<2.0.0              # FAISS vector similarity search (use faiss-gpu for CUDA)

# ---------------------------------------------------------------------------
# Phase 7: Authentication & database  ✅ Active
# ---------------------------------------------------------------------------
sqlalchemy>=2.0.0,<3.0.0             # async ORM
alembic>=1.14.0,<2.0.0               # database migrations
aiosqlite>=0.20.0,<1.0.0             # async SQLite driver (dev/test)
asyncpg>=0.30.0,<1.0.0               # async PostgreSQL driver (production)
greenlet>=3.0.0,<4.0.0               # required by SQLAlchemy async
PyJWT>=2.9.0,<3.0.0                  # JWT creation and verification
bcrypt>=4.2.0,<5.0.0                 # password hashing
email-validator>=2.2.0,<3.0.0        # pydantic EmailStr validation
